package fr.ign.cogit.geoxygene.semio.legend.metadata;

import org.junit.Assert;
import org.junit.Test;



public class SemanticRelationTest {
    
    @Test
    public void xmlTest() {
        
        // Load file
        /*SemanticRelation legend = SemanticRelation.unmarshall(SemanticRelation.class.getResource("SemanticRelation.xml").getPath());
        
        // Check 
        Assert.assertTrue("Number of leaves : " + legend.getLeaves().size(), legend.getLeaves().size() == 20);*/
    }

}
